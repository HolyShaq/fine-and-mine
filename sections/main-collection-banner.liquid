<div class="tw:flex tw:flex-col tw:gap-8 tw:mb-12">
  <img
    class="tw:w-full tw:aspect-square tw:md:aspect-[400/167] tw:object-cover"
    src="{{ collection.image | image_url }}"
    width="{{ collection.image.width }}"
    height="{{ collection.image.height }}"
  >
</div>
<div class="page-width">
  <h1 class="tw:text-6xl tw:font-bold tw:text-center tw:md:text-left">
    {{ collection.title }}
  </h1>
</div>

{% schema %}
{
  "name": "t:sections.main-collection-banner.name",
  "class": "section"
}
{% endschema %}
